---
phase: 01-code-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extract.py
  - batch_extract.sh
  - check_progress.sh
autonomous: true
requirements:
  - ALIGN-01
  - ALIGN-02
  - ALIGN-03
must_haves:
  truths:
    - "Running `python extract.py <url> --stage-only --out Inbox-Raw` places staged output in Sync_Data/_staging/tiktok/ (a sibling of Inbox-Raw, not inside it)"
    - "Image files (thumbnail.jpg, scene_*.jpg, frame_*.jpg) are written to images/ within each per-video folder"
    - "batch_extract.sh latest-stage glob resolves under Sync_Data/_staging/tiktok/ (not under OUT_DIR directly)"
    - "check_progress.sh staged-count find targets Sync_Data/_staging/tiktok"
    - "Finalize destination out_root/tiktok/tiktok-video-<id>/ (Inbox-Raw/tiktok/...) is unchanged and correct per ALIGN-03"
  artifacts:
    - "extract.py: staging root derived from out_root.parent at both call sites (_resolve_finalize_targets and main)"
    - "extract.py: all 7 image-subfolder path constructions use 'images' as the folder name"
    - "extract.py: 2 markdown body image references (thumbnail img tag and frames prose) use images/ prefix"
    - "batch_extract.sh line 90: staging glob uses ${OUT_DIR%/*} to find parent of OUT_DIR"
    - "check_progress.sh line 6: find targets /Users/joshuawallace/Data/Sync_Data/_staging/tiktok"
  key_links:
    - "_resolve_finalize_targets and main() must both derive staging root from out_root.parent so --finalize-all finds staged folders at the corrected location"
    - "extract_thumbnail, extract_frames, _copy_hive_image_assets, and _mirror_stage_images_to_assets must all reference the same images/ folder name — a mismatch means finalize copies 0 images"
---

<objective>
Surgical path alignment of extract.py and helper scripts to match the canonical 3-step pipeline storage layout defined in Revised_process_overview.md.

Two concrete discrepancies exist in the codebase today:
1. Staging root is derived from out_root (Inbox-Raw), placing it at Inbox-Raw/_staging/tiktok/ — spec requires Sync_Data/_staging/tiktok/ (a sibling of Inbox-Raw).
2. Per-video image subfolder is named assets/ in all code paths — spec requires images/.

A third location (batch_extract.sh line 90 and check_progress.sh line 6) reference the old staging path via explicit globs and must also be updated.

ALIGN-03 (finalize destination Inbox-Raw/tiktok/tiktok-video-id/) is already correct in the code — verified by Task 1 read_first. No change required.

Purpose: Ensure new extraction runs produce output at the paths that Phase 2 reprocessing and Hive intake expect. Existing extracted content is not migrated (D-02, D-05).

Output: Modified extract.py, batch_extract.sh, check_progress.sh. No new files created.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-alignment/01-CONTEXT.md
@Revised_process_overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix staging root derivation in extract.py and update shell script staging globs (D-01, ALIGN-01, ALIGN-03)</name>
  <files>extract.py, batch_extract.sh, check_progress.sh</files>
  <read_first>
    Read extract.py lines 1048–1068 (function _resolve_finalize_targets — contains the first staging root assignment) and lines 1409–1415 (main() — contains the second staging root assignment and mkdir).
    Read extract.py line 999 (`final_dir = out_root / SOURCE_TYPE / asset_id`) to confirm ALIGN-03 finalize destination is already correct — no change needed there.
    Read batch_extract.sh lines 85–95 (the ls -td command that looks up the latest staged directory at line 90).
    Read check_progress.sh line 6 (the find command that counts staged dirs).
  </read_first>
  <action>
    Make three targeted edits. No new CLI flags, no new env vars, no migration of existing content (D-03, D-02).

    Edit 1 — extract.py, _resolve_finalize_targets (~line 1053):
    The assignment that builds staged_root by joining out_root with "_staging" and SOURCE_TYPE currently anchors under out_root. Change the base from out_root to out_root.parent. This moves staging from Inbox-Raw/_staging/tiktok/ to Sync_Data/_staging/tiktok/ per the spec. The rest of the function is unchanged.

    Edit 2 — extract.py, main() (~line 1412):
    The assignment that builds staging_root by joining out_root with "_staging" currently anchors under out_root. Change the base from out_root to out_root.parent. The mkdir on the following line is unchanged (Sync_Data always exists; mkdir -p handles the _staging subdirectory). Per D-01: out_root is always Sync_Data/Inbox-Raw so out_root.parent is Sync_Data.

    Edit 3 — batch_extract.sh, line 90:
    The `ls -td` command builds a glob under `"$OUT_DIR"/_staging/`. OUT_DIR is set to Inbox-Raw at the top of the script. Change the glob anchor to the parent of OUT_DIR. In bash, `${OUT_DIR%/*}` strips the last path component. Replace `"$OUT_DIR"/_staging/tiktok/tiktok-video-*/` with `"${OUT_DIR%/*}"/_staging/tiktok/tiktok-video-*/`. Keep the rest of the line identical.

    Edit 4 — check_progress.sh, line 6:
    The find command currently targets the absolute path ending in Inbox-Raw/_staging/tiktok. Change the path to /Users/joshuawallace/Data/Sync_Data/_staging/tiktok (remove the Inbox-Raw component). The rest of the find invocation (maxdepth, type, name pattern) is unchanged.
  </action>
  <verify>
    <automated>python3 -c "
code = open('extract.py').read()
n = code.count('out_root.parent / \"_staging\"')
assert n == 2, f'Expected 2 staging root corrections, got {n}'
finalize_ok = 'out_root / SOURCE_TYPE / asset_id' in code
assert finalize_ok, 'Finalize destination line missing'
print(f'extract.py: {n} staging root sites corrected; finalize destination intact')
" &amp;&amp; grep -c 'OUT_DIR%' batch_extract.sh &amp;&amp; grep -c '/Sync_Data/_staging/tiktok' check_progress.sh</automated>
  </verify>
  <acceptance_criteria>
    - extract.py has exactly 2 occurrences of `out_root.parent / "_staging"` (one in _resolve_finalize_targets, one in main)
    - extract.py line ~999 still reads `out_root / SOURCE_TYPE / asset_id` (finalize destination unchanged — ALIGN-03 satisfied)
    - batch_extract.sh line 90 ls command references `${OUT_DIR%/*}/_staging/tiktok/tiktok-video-*/`
    - check_progress.sh line 6 find targets `/Users/joshuawallace/Data/Sync_Data/_staging/tiktok`
  </acceptance_criteria>
  <done>
    Staging path in extract.py resolves to Sync_Data/_staging/tiktok/ for all new runs. Shell script staging globs are consistent. Finalize destination Inbox-Raw/tiktok/tiktok-video-ID/ is confirmed unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename image subfolder from assets to images throughout extract.py (D-04, D-06, ALIGN-02)</name>
  <files>extract.py</files>
  <read_first>
    Read extract.py lines 533–547 (extract_thumbnail — first image path construction).
    Read extract.py lines 675–690 (extract_frames — frames_dir setup).
    Read extract.py lines 847–888 (_copy_hive_image_assets and _mirror_stage_images_to_assets — four path constructions).
    Read extract.py lines 1032–1043 (_finalize_stage_dir rollback section — seventh path construction).
    Read extract.py lines 1270–1282 (write_contract_content Media section — two markdown body string literals).
  </read_first>
  <action>
    Make 9 targeted edits to extract.py. These are the complete set of locations where the per-video image subfolder name appears as a path component. Apply to new runs only — no migration of existing content (D-05). Do not rename any other directory (CENTRALIZED_ASSETS_ROOT, _assets root, staging, etc.).

    Seven path-construction edits (using the Path / operator):

    1. extract_thumbnail (~line 536): the assignment joining out_dir with the image folder name for thumbnail extraction — change the folder name from the old name to `images`.

    2. extract_frames (~line 683): the frames_dir assignment joining out_dir with the image folder name — change the folder name to `images`.

    3. _copy_hive_image_assets (~line 852): source_assets_dir joining asset_payload_dir with the image folder name — change to `images`.

    4. _copy_hive_image_assets (~line 856): inbox_assets_dir joining inbox_item_dir with the image folder name — change to `images`.

    5. _mirror_stage_images_to_assets (~line 874): stage_assets joining stage_dir with the image folder name — change to `images`.

    6. _mirror_stage_images_to_assets (~line 878): target_assets joining asset_dir with the image folder name — change to `images`.

    7. _finalize_stage_dir rollback (~line 1040): inbox_assets_dir joining final_dir with the image folder name (used to roll back copied images on error) — change to `images`.

    Two markdown body string literal edits in write_contract_content:

    8. ~Line 1275: the f-string generating the thumbnail img tag contains `assets/{thumb_path.name}` as the relative image path — change `assets/` to `images/`.

    9. ~Line 1278: the prose string referencing the image folder by name in the "Extracted Frames" section — change the folder name reference from `assets/` to `images/`.

    Per Claude's discretion: add a brief inline comment on the first renamed assignment (extract_thumbnail) noting the alignment — for example `# images/ per Revised_process_overview.md`. One line, no more.
  </action>
  <verify>
    <automated>python3 -c "
code = open('extract.py').read()
n_path = code.count('/ \"images\"')
assert n_path == 7, f'Expected 7 path-join occurrences with images subfolder, got {n_path}'
assert 'images/' in code, 'images/ not found in markdown body'
print(f'OK: {n_path} path constructions and markdown body updated to images/')
"</automated>
  </verify>
  <acceptance_criteria>
    - extract.py has exactly 7 occurrences of the images subfolder name joined via the Path / operator (at lines ~536, ~683, ~852, ~856, ~874, ~878, ~1040)
    - The markdown body thumbnail img tag (~line 1275) uses `images/{filename}` as the relative image link
    - The markdown body frames prose (~line 1278) references the `images/` folder name
    - None of the 7 original path-construction locations retain the old subfolder name
    - CENTRALIZED_ASSETS_ROOT, staging paths, and the `_assets` root folder are untouched
  </acceptance_criteria>
  <done>
    All image file writes (thumbnail, scene frames, interval frames) target images/ within each video folder. _copy_hive_image_assets and _mirror_stage_images_to_assets read from and write to images/. Finalize rollback references images/. Contract markdown uses relative images/ links. New extractions produce tiktok-video-ID/images/ layout matching the canonical spec in Revised_process_overview.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem write | extract.py writes to local Sync_Data paths; no external network calls in these code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Incorrect path derivation | low | mitigate | Both staging root sites changed to out_root.parent; verify gate confirms exactly 2 occurrences |
| T-01-02 | Tampering | Partial rename (missed location) | low | mitigate | Positive grep gate asserts exactly 7 path-join occurrences post-rename; manual review of CONTEXT.md inventory |
| T-01-03 | Denial of Service | Wrong path causes all staged content to be invisible to finalize | medium | mitigate | Task 1 verify checks both sites are corrected; Task 2 verify checks all 7 image path sites |
| T-01-04 | Information Disclosure | No sensitive data touched | low | accept | Only file path strings are modified; no credentials, tokens, or PII involved |
</threat_model>

<verification>
After both tasks complete, run the combined verification:

```bash
python3 -c "
code = open('extract.py').read()

# ALIGN-01: staging root uses parent
n_staging = code.count('out_root.parent / \"_staging\"')
assert n_staging == 2, f'staging: expected 2, got {n_staging}'

# ALIGN-02: image subfolder path joins
n_images = code.count('/ \"images\"')
assert n_images == 7, f'images path joins: expected 7, got {n_images}'

# ALIGN-03: finalize destination unchanged
assert 'out_root / SOURCE_TYPE / asset_id' in code, 'finalize destination missing'

# Markdown body
assert 'images/' in code, 'images/ missing from markdown body'

print(f'All checks pass: {n_staging} staging sites, {n_images} image path joins, finalize destination intact')
"

grep -c 'OUT_DIR%' batch_extract.sh
grep -c '/Sync_Data/_staging/tiktok' check_progress.sh
```

All assertions must pass with no failures.
</verification>

<success_criteria>
1. `python3 -c "code=open('extract.py').read(); assert code.count('out_root.parent / \"_staging\"') == 2"` exits 0
2. `python3 -c "code=open('extract.py').read(); assert code.count('/ \"images\"') == 7"` exits 0
3. `grep -c 'OUT_DIR%' batch_extract.sh` returns 1
4. `grep -c '/Sync_Data/_staging/tiktok' check_progress.sh` returns 1
5. New extraction run (dry-run test if feasible): staged folder appears at Sync_Data/_staging/tiktok/tiktok-video-ID/ with images/ subfolder
</success_criteria>

<output>
Create `.planning/phases/01-code-alignment/01-01-SUMMARY.md` when done.
</output>
