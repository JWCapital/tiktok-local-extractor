---
phase: 02-reprocess-remaining-videos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - batch_extract.sh
autonomous: true
requirements:
  - REPROCESS-01
must_haves:
  truths:
    - "batch_extract.sh line-90 staging glob resolves to Sync_Data/_staging/tiktok (parent of Inbox-Raw), so description-link logging finds the folders extract.py --stage-only actually produces (D-04)"
  artifacts:
    - "batch_extract.sh — line-90 staging-glob prefix changed to ${OUT_DIR%/*}"
  key_links:
    - "batch_extract.sh link-extraction glob → _staging/tiktok/ staged folders written by extract.py --stage-only"
---

<objective>
Apply the locked one-line fix (D-04) to `batch_extract.sh` so the link-extraction
staging glob points at the correct staging root.

After Phase 1, `extract.py --stage-only` writes staged folders to
`Sync_Data/_staging/tiktok/` (the parent of `Inbox-Raw/`). But line 90 of
`batch_extract.sh` still globs under `$OUT_DIR` (which equals
`Sync_Data/Inbox-Raw`), so the post-stage description-link logging finds nothing
and silently skips. The fix changes the glob prefix from the full `$OUT_DIR`
expansion to `${OUT_DIR%/*}` (strip the trailing `/Inbox-Raw` → `Sync_Data`).

Purpose: Make the batch runner's link-logging step consistent with Phase 1's
corrected staging path before the full reprocess run in Plan 02.
Output: A single-line edit to `batch_extract.sh`, verifiable by grep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reprocess-remaining-videos/02-CONTEXT.md
@.planning/phases/01-code-alignment/01-VERIFICATION.md
@batch_extract.sh
@Revised_process_overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the link-extraction staging glob prefix on batch_extract.sh line 90 (D-04)</name>
  <files>batch_extract.sh</files>
  <read_first>
    - batch_extract.sh — read the full script; confirm line 16 sets `OUT_DIR="/Users/joshuawallace/Data/Sync_Data/Inbox-Raw"` and that line 90 is the `latest_stage=$(ls -td ... )` assignment inside the post-stage link block.
    - .planning/phases/01-code-alignment/01-VERIFICATION.md — confirms extract.py now stages to `out_root.parent / "_staging"` (i.e. `Sync_Data/_staging/tiktok`), which is why the line-90 prefix must be the parent of `$OUT_DIR`.
    - .planning/phases/02-reprocess-remaining-videos/02-CONTEXT.md — decision D-04 (exact change required).
  </read_first>
  <action>
    On line 90 of `batch_extract.sh`, change the staging-glob prefix so it is
    rooted at the parent of `$OUT_DIR` instead of `$OUT_DIR` itself.

    Currently the glob is prefixed with the full `$OUT_DIR` variable (which
    resolves to `Sync_Data/Inbox-Raw`). Replace that prefix with the
    parameter-stripped form `${OUT_DIR%/*}` (which resolves to `Sync_Data`, the
    parent directory), so the staged folders produced by `extract.py --stage-only`
    are found.

    The line must read EXACTLY (preserve the existing 4-space indentation):

        latest_stage=$(ls -td "${OUT_DIR%/*}"/_staging/tiktok/tiktok-video-*/ 2>/dev/null | head -1)

    This is a single in-place edit to line 90 ONLY. Do NOT add a comment, do NOT
    touch any other line, and do NOT change the `--out "$OUT_DIR"` arguments on
    lines 85 and 121 (those correctly pass the Inbox-Raw root to extract.py).
  </action>
  <verify>
    <automated>grep -Fq 'ls -td "${OUT_DIR%/*}"/_staging/tiktok/tiktok-video-*/' batch_extract.sh && [ "$(grep -v '^#' batch_extract.sh | grep -cF 'ls -td "$OUT_DIR"/_staging')" = "0" ] && bash -n batch_extract.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'ls -td "${OUT_DIR%/*}"/_staging/tiktok/tiktok-video-*/' batch_extract.sh` returns exactly `1` (the corrected glob is present).
    - `grep -v '^#' batch_extract.sh | grep -cF 'ls -td "$OUT_DIR"/_staging'` returns `0` (the old Inbox-Raw-rooted glob no longer exists in any non-comment line).
    - `bash -n batch_extract.sh` exits 0 (script still parses; no syntax error introduced).
    - `git diff --numstat batch_extract.sh` shows exactly `1` line added and `1` line removed (single-line change, nothing else touched).
    - Lines 85 and 121 still contain `--out "$OUT_DIR"` unchanged (`grep -c -- '--out "$OUT_DIR"' batch_extract.sh` returns `2`).
  </acceptance_criteria>
  <done>
    Line 90 of batch_extract.sh uses the `${OUT_DIR%/*}` staging-glob prefix, the
    script passes `bash -n`, and the diff is a single line changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan is a local single-line source edit; no untrusted input is processed and no network/credential boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-E01 | Tampering | batch_extract.sh line 90 | low | mitigate | Exact-string grep acceptance criteria verify the precise one-line change; `git diff --numstat` confirms only one line moved; `bash -n` confirms no syntax regression. |

No package installs in this plan — supply-chain (T-02-SC) gate is N/A.
</threat_model>

<artifacts>
## Artifacts this plan produces

**Modified files:**
- `batch_extract.sh` — line 90 staging-glob prefix changed from `$OUT_DIR` to `${OUT_DIR%/*}`.

No new symbols, files, or directories are created by this plan.
</artifacts>

<verification>
- Corrected glob present on line 90 (`${OUT_DIR%/*}` prefix).
- Old Inbox-Raw-rooted glob absent.
- Script parses (`bash -n`).
- Diff is a single line.
</verification>

<success_criteria>
batch_extract.sh's link-extraction step globs the correct staging root
(`Sync_Data/_staging/tiktok/`), the change is exactly one line, and the script
still parses — unblocking the batch reprocess run in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/02-reprocess-remaining-videos/02-01-SUMMARY.md` when done.
</output>
