---
phase: "01-todo-capture-foundation"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
   - ".planning/todos/pending/.gitkeep"
   - ".planning/todos/pending/_TEMPLATE.todo.md"
   - ".planning/todos/README.md"
   - ".planning/ROADMAP.md"
autonomous: true
requirements:
   - "TODO-01"
   - "TODO-02"
user_setup: []
must_haves:
   truths:
      - "User can capture a pending todo item with title, area, and problem/solution notes."
      - "Captured todo items are persisted in `.planning/todos/pending/` with consistent frontmatter."
      - "Persisted todo entries can be reopened later without field loss."
   artifacts:
      - ".planning/todos/pending/_TEMPLATE.todo.md: canonical pending todo schema and section structure"
      - ".planning/todos/README.md: capture rules, required fields, and naming guidance"
      - ".planning/todos/pending/.gitkeep: repository-tracked pending queue directory"
   key_links:
      - "gsd-capture output -> .planning/todos/pending/ (per D-04)"
      - "frontmatter keys (`created`, `title`, `area`, `status`, `files`) -> TODO-02 persistence contract (per D-02)"
      - "markdown body sections (`Problem`, `Solution`, `Notes`) -> TODO-01 capture contract (per D-03)"
---

# Plan 01-01

<!-- markdownlint-disable MD033 -->

<objective>
Deliver Phase 1 todo-capture foundation so pending planning items can be created and stored consistently.

Purpose: satisfy TODO-01 and TODO-02 with a deterministic pending queue format that remains local-first and human-editable.
Output: pending queue directory scaffolding, canonical todo template, and capture guidance aligned with Phase 1 decisions.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-todo-capture-foundation/01-CONTEXT.md
@.planning/phases/999.1-update-extractor-based-on-this-information-ingestion-templat/999.1-01-PLAN.md
@.planning/phases/999.1-update-extractor-based-on-this-information-ingestion-templat/999.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
   <name>Task 1: Establish pending todo storage and canonical schema</name>
   <files>.planning/todos/pending/.gitkeep, .planning/todos/pending/_TEMPLATE.todo.md</files>
   <action>Create `.planning/todos/pending/` as the canonical pending queue and add a template that uses YAML frontmatter + markdown body (per D-01). Require frontmatter keys `created`, `title`, `area`, `status`, `files` (per D-02), with `status` defaulting to `pending`. Define body sections `## Problem`, `## Solution`, and `## Notes` (per D-03). Keep schema strict enough for consistency but human-editable for local-first workflows.</action>
   <verify>
      <automated>test -d .planning/todos/pending && test -f .planning/todos/pending/_TEMPLATE.todo.md && grep -E "^(created|title|area|status|files):" .planning/todos/pending/_TEMPLATE.todo.md | wc -l | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
   </verify>
   <done>Template and queue directory exist with required frontmatter fields and body sections.</done>
</task>

<task type="auto">
   <name>Task 2: Document capture contract for gsd-capture output</name>
   <files>.planning/todos/README.md</files>
   <action>Add `.planning/todos/README.md` defining how `gsd-capture` writes pending todo files to `.planning/todos/pending/` (per D-04), including naming convention, required frontmatter semantics, and lenient capture validation behavior that persists entries with warnings when fields are incomplete (per D-05). Explicitly describe soft dedupe warning behavior for title+area similarity while preserving user control over merge/defer outcomes (per D-06). Keep scope limited to capture/persistence only; do not add triage or roadmap-linkage behavior from Phases 2/3.</action>
   <verify>
      <automated>test -f .planning/todos/README.md && grep -Ei "gsd-capture|pending|lenient|dedupe|title\+area" .planning/todos/README.md | wc -l | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
   </verify>
   <done>Capture contract is documented with Phase 1-only behavior and decision traceability.</done>
</task>

<task type="auto">
   <name>Task 3: Add Phase 1 usage example and verification-ready checklist references</name>
   <files>.planning/todos/README.md, .planning/ROADMAP.md</files>
   <action>Add one concrete example pending todo entry in README showing required frontmatter and Problem/Solution/Notes shape, plus a short verification checklist for creating and reopening an item unchanged. Update Phase 1 roadmap plan listing to reference this implementation plan file and keep progress as planned-not-executed.</action>
   <verify>
      <automated>grep -F "01-01-PLAN.md" .planning/ROADMAP.md && grep -Ei "Problem|Solution|Notes|reopen" .planning/todos/README.md | wc -l | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
   </verify>
   <done>Phase 1 has an executable plan reference and practical verification guidance without execution claims.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

- `user input → todo file frontmatter/body`: Untrusted capture content crosses into persisted markdown artifacts.
- `capture command output → pending queue filesystem`: File writes can overwrite or create malformed records if conventions are not enforced.

## STRIDE Threat Register

- `T-01-01` (Tampering, `.planning/todos/pending/*.md`) — **mitigate**: enforce canonical template keys/sections and rely on git history.
- `T-01-02` (Repudiation, pending todo records) — **mitigate**: require `created` timestamp and stable naming guidance.
- `T-01-03` (Information Disclosure, todo notes content) — **accept**: local-first workflow with no external sync added in Phase 1.
- `T-01-SC` (Tampering, package installs) — **accept**: no package-manager install tasks in this plan.

</threat_model>

<verification>
- [ ] Create one pending todo file from the template and confirm required frontmatter is present.
- [ ] Persist the file under `.planning/todos/pending/` and reopen it to confirm content is unchanged.
- [ ] Confirm README capture guidance includes D-04, D-05, and D-06 behaviors.
- [ ] Confirm roadmap references `01-01-PLAN.md` and does not mark Phase 1 execution complete.
</verification>

<success_criteria>

- TODO-01 and TODO-02 are both covered by concrete, file-backed capture conventions.
- `.planning/todos/pending/` exists and is repo-tracked.
- Canonical pending todo template includes required frontmatter + Problem/Solution/Notes sections.
- Capture behavior documentation is phase-scoped (capture/persistence only) and explicitly excludes Phase 2/3 functionality.

</success_criteria>

<output>
After completion, create `.planning/phases/01-todo-capture-foundation/01-01-SUMMARY.md`
</output>

<!-- markdownlint-enable MD033 -->
