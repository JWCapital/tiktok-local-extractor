---
phase: "999.1-update-extractor-based-on-this-information-ingestion-templat"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
   - "extract.py"
   - "README.md"
   - "requirements.txt"
autonomous: true
requirements:
   - "EXTRACT-CT-01"
   - "EXTRACT-CT-02"
   - "EXTRACT-CT-03"
   - "EXTRACT-CT-04"
   - "EXTRACT-CT-05"
   - "EXTRACT-CT-06"
   - "EXTRACT-CT-07"
   - "EXTRACT-CT-08"
   - "EXTRACT-CT-09"
user_setup: []
must_haves:
   truths:
      - "Extractor outputs must conform to TikTok requirements in INGESTION_TEMPLATE.md."
      - "Rights-first and caption-first behavior must remain intact."
      - "No partial asset writes; stage then atomically move to final destination."
      - "Deduplication is extractor-owned via source_hash + persistent ledger."
      - "New contract-compliant runs target inbox-style structure and include required frontmatter fields."
      - "Default `zone: personal` is applied with explicit override support to `work`."
   artifacts:
      - "extract.py: contract-compliant TikTok extraction pipeline"
      - "README.md: operator documentation for contract output and migration"
      - "requirements.txt: dependencies for contract validation and metadata generation"
   key_links:
      - "extract.py -> inbox-raw/.staging/[asset_id] via stage-first atomic move"
      - "extract.py -> ~/.extractors/tiktok/extraction-history.json via source_hash dedupe ledger"
      - "extract.py -> inbox-raw/_extraction_errors/tiktok/[asset_id]-error.json via structured failure record"
---

# Plan 999.1-01

<!-- markdownlint-disable MD033 -->

<objective>
Refactor `extract.py` so new extractions comply with the TikTok ingestion contract in `/Users/joshuawallace/Data/Commands/INGESTION_TEMPLATE.md`.

Purpose: make outputs ingestion-ready without breaking rights-first and caption-first guarantees.
Output: contract-compliant asset layout, metadata/frontmatter, dedupe/ledger/error handling, and updated docs.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@extract.py
@README.md
@requirements.txt
@/Users/joshuawallace/Data/Commands/INGESTION_TEMPLATE.md
</context>

<tasks>

<task type="auto">
   <name>Task 1: Implement contract identity and atomic staging flow</name>
   <files>extract.py, README.md</files>
   <read_first>extract.py, README.md, /Users/joshuawallace/Data/Commands/INGESTION_TEMPLATE.md</read_first>
   <action>Refactor output pathing so new assets use `tiktok-video-[tiktok_video_id]` identity and are written to a staging directory first (`inbox-raw/.staging/[asset_id]/`) before a single atomic move to the final destination (`inbox-raw/tiktok/[asset_id]/`). Preserve existing rights gate (`--rights`) and caption-first fallback behavior. Add explicit compatibility note that legacy `exports/` folders remain untouched unless manually migrated.</action>
   <verify>Run one extraction in dry mode (or minimal real run) and confirm staging path is created then moved to final path with no partial final folder artifacts.</verify>
   <acceptance_criteria>
      - `extract.py` contains explicit staging and final destination handling for `inbox-raw/.staging` and `inbox-raw/tiktok`.
      - New asset naming uses `tiktok-video-` prefix with source video ID.
      - Rights-first/caption-first flow is still present and not bypassed.
   </acceptance_criteria>
   <done>New extraction writes are atomic and contract destination paths are in place.</done>
</task>

<task type="auto">
   <name>Task 2: Add ledger, dedupe, and structured error recording</name>
   <files>extract.py, requirements.txt</files>
   <read_first>extract.py, /Users/joshuawallace/Data/Commands/INGESTION_TEMPLATE.md</read_first>
   <action>Implement persistent extractor-owned ledger at `~/.extractors/tiktok/extraction-history.json`, generate `source_hash = SHA256(tiktok_video_id + uploader_handle + duration)`, and gate creation on dedupe status. Record each extraction run with counts, new_assets, duplicates, and errors. On failures, emit structured error records compatible with `_extraction_errors` semantics containing run id, source id, error type/message/context, retry metadata, and action_required guidance.</action>
   <verify>Run extraction twice for the same source and confirm second run is logged as duplicate skip; trigger one controlled failure and confirm structured error JSON is written.</verify>
   <acceptance_criteria>
      - Ledger file exists at `~/.extractors/tiktok/extraction-history.json` and appends run entries.
      - Duplicate detection uses `source_hash` and skips duplicate asset creation.
      - Failure path creates structured error record with retry and context fields.
   </acceptance_criteria>
   <done>Extractor behavior is idempotent and auditable via ledger + error logs.</done>
</task>

<task type="auto">
   <name>Task 3: Emit contract-compliant frontmatter and validation checks</name>
   <files>extract.py, README.md</files>
   <read_first>extract.py, README.md, /Users/joshuawallace/Data/Commands/INGESTION_TEMPLATE.md</read_first>
   <action>Update generated markdown (`content.md` or equivalent contract target) to include required source identification, content classification, extraction tracking, TikTok-specific metadata, `supporting_files`, and `quality_checks`. Implement explicit zone behavior: default `zone: personal` for TikTok outputs with configurable override to `zone: work`. Add pre-finalization validation to ensure required fields, relative paths, and supporting-file existence. Update README with the contract field map and operational migration notes from legacy outputs to contract-compliant runs.</action>
   <verify>Generate one asset and validate required fields: `source_type`, `source_id`, `source_system`, `extracted_at`, `captured_at`, `extraction_run_id`, `processor_id`, `processor_version`, `source_hash`, `extraction_status`, `supporting_files`, and `quality_checks`; verify default zone is `personal` and override to `work` is honored; verify all referenced files exist relative to asset folder.</verify>
   <acceptance_criteria>
      - Generated frontmatter includes required TikTok contract fields and tracking metadata.
      - `supporting_files` and `quality_checks` sections are present and accurate.
      - Zone behavior is explicit: default `personal`, override path to `work` verified.
      - README documents contract output structure and migration/compatibility behavior.
   </acceptance_criteria>
   <done>Output metadata is ingestion-contract compliant and verifiable from disk.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run one extraction and verify atomic staging-to-final move behavior.
- [ ] Re-run same source and verify ledger duplicate skip behavior.
- [ ] Validate generated metadata/frontmatter and supporting file references against template requirements.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No rights-first/caption-first regression introduced
- New extractor output is contract-compliant for TikTok assets and traceable via persistent ledger

</success_criteria>

<output>
After completion, create `.planning/phases/999.1-update-extractor-based-on-this-information-ingestion-templat/999.1-01-SUMMARY.md`
</output>

<!-- markdownlint-enable MD033 -->
